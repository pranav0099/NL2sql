name: ns
description: A hyperlocal product discovery platform
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # Phase 1
  go_router: ^14.0.0
  dio: ^5.0.0
  flutter_secure_storage: ^9.0.0

  # Phase 4 - New dependencies
  stomp_dart_client: ^0.4.0
  firebase_core: ^3.0.0
  firebase_messaging: ^15.0.0
  flutter_local_notifications: ^17.0.0
  rxdart: ^0.27.0
  freezed_annotation: ^2.0.0
  json_annotation: ^4.0.0
  flutter_riverpod: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.0.0
  freezed: ^2.0.0
  json_serializable: ^6.0.0

flutter:
  uses-material-design: true
